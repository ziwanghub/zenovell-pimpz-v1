# M9.5D Legacy Cleanup Report

Date: `2026-07-06`
Phase: `M9.5D Legacy Cleanup After Header Extraction`
Status: `CLOSED`
Authority:

- `docs/architecture/M9.5B-HEADER-EXTRACTION-ARCHITECTURE.md`
- `docs/reports/m9.5c-header-extraction/M9.5C-CLOSEOUT.md`

## What Legacy Was Removed

- Removed the unused file-local `HeroHeader` runtime implementation from `sections/hero/hero-section.tsx`.
- Removed the temporary `showHeader` compatibility prop and legacy render branch from `HeroSection`.
- Removed `heroContent.header.lineCta`.
- Removed `heroContent.header.menuLabel`.
- Removed stale legacy comments in `content/hero.ts` that described `heroContent.header` as an active runtime authority.
- Updated `content/site-header.ts` authority metadata to reflect that:
  - GlobalHeader is live
  - Hero header runtime has been removed
  - Hero header fields have been removed from Hero content

## What Legacy Was Intentionally Retained

- `content/hero.ts` file name remains unchanged.
- `heroContent.brand.wordmark`, `heroContent.brand.tagline`, and `heroContent.brand.subbrand` remain in place because they still belong to Section 1 Hero body content.
- GlobalHeader runtime, mapper, and drawer foundation remain unchanged except for previously approved accessibility/runtime hardening.
- Footer navigation remains section-local and was not migrated.

## Reference Search Results

Runtime reference outcome:

- No runtime references remain to:
  - `HeroHeader`
  - `showHeader`
  - `heroContent.header`
  - `headerLineCta`
  - `headerMenuLabel`

Remaining matches are documentation/history only and are expected:

- architecture decision log / historical migration notes
- prior closeout report references

Raw search evidence:

- `docs/reports/m9.5d-legacy-cleanup/reference-search.txt`

## Validation Result

- `npm run lint` PASS
- `npm run typecheck` PASS
- `npm run build` PASS
- `npm run validate` PASS

## Browser QA Result

Validated at:

- `375px`
- `390px`
- `414px`
- `430px`

Verified at all widths:

- one visible header only
- legacy header count = `0`
- header top = `0`
- header height = `61px`
- Hero body start = `80px`
- safe-area padding preserved
- drawer still opens
- drawer closes with `Escape`
- focus returns to the menu trigger after close
- no horizontal overflow
- Footer remains present

Evidence:

- `docs/reports/m9.5d-legacy-cleanup/qa-summary.json`
- `docs/reports/m9.5d-legacy-cleanup/m95d-*-closed.png`
- `docs/reports/m9.5d-legacy-cleanup/m95d-*-open.png`

## Regression Result

- Section 1 visual baseline remains unchanged apart from removal of dead header code.
- Sections `2–11` remain untouched.
- Footer remains unchanged.
- `MobileShell` remains unchanged.
- Drawer behavior remains intact.
- No horizontal scroll regression detected.

## Rename Decision

- Rename of `content/hero.ts` is still deferred.
- It is not required for this cleanup pass.
- Deferring the rename avoids unnecessary import churn outside the scope of legacy header cleanup.

## Closeout Status

- `M9.5D` legacy cleanup is complete.
- Header extraction runtime is now free of the old Hero-owned header path.
- Project is ready to move beyond header extraction cleanup without carrying dead runtime header code.
