# M9.5C Header Extraction Closeout

Date: `2026-07-06`
Phase: `M9.5C Header Extraction Implementation`
Status: `CLOSED`
Architecture Authority: `docs/architecture/M9.5B-HEADER-EXTRACTION-ARCHITECTURE.md`

## Scope

- `M9.5C-P1` GlobalHeader Foundation
- `M9.5C-P2` Header Integration + Hero Parity QA
- `M9.5C-P3` Header Stabilization + Closeout

## Final QA Summary

- GlobalHeader is the only visible runtime header.
- Legacy `HeroHeader` does not render in normal runtime.
- GlobalHeader is mounted outside Hero ownership and above Section 1 inside `MobileShell`.
- HeroSection renders as Hero body only during active GlobalHeader runtime.
- No double-header state was detected.
- No `MobileShell` change was required.
- No Footer runtime migration was performed.
- No frozen Sections `2–11` were modified.

## Viewport Evidence Summary

Validated at:

- `375px`
- `390px`
- `414px`
- `430px`

Verified at all widths:

- header count = `1`
- header top = `0`
- header height = `61px`
- spacer height = `80px`
- hero body starts at `80px`
- safe-area padding remains `env(safe-area-inset-top)`
- no horizontal overflow
- footer remains present

Evidence sources:

- `docs/reports/m9.5c-p2-header-integration/qa-summary.json`
- `docs/reports/m9.5c-p2-header-integration/focus-trap-summary.json`
- `docs/reports/m9.5c-p2-header-integration/p2-*-closed.png`
- `docs/reports/m9.5c-p2-header-integration/p2-*-open.png`

## Accessibility Summary

- Menu trigger has `aria-label`.
- Menu trigger has `aria-expanded`.
- Menu trigger has `aria-controls` pointing to the real drawer panel id.
- Drawer panel uses `role="dialog"` with `aria-modal="true"` and `aria-labelledby`.
- Focus moves into the drawer on open.
- Focus returns to the menu trigger after close.
- `Tab` and `Shift+Tab` remain trapped inside the drawer while open.
- Closed drawer unmounts from the DOM rather than remaining as an exposed hidden dialog.

## Drawer Stability Summary

- Drawer opens from the menu trigger.
- Drawer closes from the close button.
- Drawer closes with `Escape`.
- Drawer closes with backdrop click.
- Body scroll lock applies only while the drawer is open.
- Body scroll lock clears after drawer close.
- Drawer portal is not clipped by `MobileShell`.
- No horizontal scroll appears while the drawer is open.
- Z-index layering remains consistent with the architecture authority.

## Regression Summary

- Footer remains visually unchanged.
- Sections `2–11` remain untouched.
- No verified layout shift across the page outside the approved fixed-header spacer model.
- No build, typecheck, lint, or validate regression was introduced.
- Rollback path remains valid because:
  - `content/hero.ts` is unchanged structurally
  - `heroContent.header` remains present
  - legacy Hero runtime code still exists in Section 1
  - Footer migration has not started
  - `MobileShell` is unchanged

## Validation Summary

- `npm run lint` PASS
- `npm run typecheck` PASS
- `npm run build` PASS
- `npm run validate` PASS

## Closeout Status

- `M9.5C` Header Extraction Implementation is closed.
- GlobalHeader extraction is stable within the approved scope.
- `M9.5D Legacy Cleanup` has not started and remains the next separate governance-controlled phase.

## Known Limitations

- Legacy Hero header fields remain intentionally in place.
- Footer still consumes section-local navigation authority.
- No desktop navigation has been introduced.
- No animation polish has been introduced.
